import React from 'react'
import PropTypes from 'prop-types'

const tabsBar = props => {
    return (
        <div className='cl-tab__bar' >
            
        </div>
    )
}

tabsBar.propTypes = {
    
}

export default tabsBar
